runtime = "python310"

[env]
PYTHONUNBUFFERED = "1"

[build]
builder = "dockerfile"

[deploy]
startCommand = "cd apps/agents && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyMaxRetries = 10
