runtime = "python310"

[env]
PYTHONUNBUFFERED = "1"

[build]
builder = "nixpacks"

[deploy]
startCommand = "cd apps/agents && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyMaxRetries = 10
